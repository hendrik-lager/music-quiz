# Spotify Developer App credentials
# Create an app at https://developer.spotify.com/dashboard
SPOTIFY_CLIENT_ID=your_spotify_client_id
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret

# App hostname used by Traefik to route requests (e.g. quiz.example.com)
APP_HOST=quiz.example.com

# Redirect URI must match exactly what you registered in the Spotify Developer Dashboard
SPOTIFY_REDIRECT_URI=https://quiz.example.com/auth/spotify/callback

# Server config
PORT=8080
HOST=0.0.0.0

# Secret key for session security (generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))")
SECRET_KEY=change_me_to_a_random_secret
